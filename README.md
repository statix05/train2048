# 🎮 2048 AI - Neural Network Edition

Игра 2048 с оригинальным интерфейсом и обучаемой нейросетью на основе **Dueling DQN** с **Attention механизмом**.

![Python](https://img.shields.io/badge/Python-3.9+-blue.svg)
![PyTorch](https://img.shields.io/badge/PyTorch-2.0+-red.svg)
![Platform](https://img.shields.io/badge/Platform-macOS%20%7C%20Linux%20%7C%20Windows-green.svg)

## 🚀 Быстрый старт (macOS Apple Silicon)

### 1. Создайте виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate
```

### 2. Установите зависимости
```bash
# PyTorch для Apple Silicon (M1/M2/M3)
pip install torch torchvision torchaudio

# NumPy
pip install numpy
```

### 3. Проверьте поддержку MPS
```bash
python -c "import torch; print(f'MPS available: {torch.backends.mps.is_available()}')"
```

### ⚠️ Проблемы с GUI?

Если видите ошибку `_tkinter`, читайте [INSTALL_MACOS.md](INSTALL_MACOS.md).

**Быстрое решение:** используйте терминальный интерфейс (работает без Tkinter):
```bash
python gui_terminal.py
```

## 🎯 Использование

### Ручная игра

**GUI (если Tkinter установлен):**
```bash
python main.py play
```

**Терминал (работает всегда):**
```bash
python gui_terminal.py
```

**Управление:**
- **Стрелки (↑↓←→)** — движение плиток
- **R** — рестарт
- **A** — включить/выключить AI
- **Q/ESC** — выход

### Наблюдение за AI
```bash
python main.py play --ai
```

### Обучение нейросети

**С визуализацией:**
```bash
# Training GUI (если Tkinter работает)
python main.py train-gui

# Terminal Training (работает всегда) ⭐ НОВОЕ!
python main.py train-terminal
python training_terminal.py --episodes 1000

# Или добавить флаг --gui к обычной команде
python main.py train --quick --gui
```

**Консольное обучение:**
```bash
# Полное обучение (5000 эпизодов)
python main.py train

# Быстрое обучение для тестирования (500 эпизодов)
python main.py train --quick

# Продолжить обучение существующей модели
python main.py train --resume

# Настройка параметров
python main.py train --episodes 10000 --lr 0.0001 --batch-size 128
```

**Training GUI показывает:**
- 🎮 Игровое поле в реальном времени
- 📊 Графики Score, Max Tile, Loss
- 📈 Статистику обучения (средние и лучшие результаты)
- ⚙️ Управление скоростью визуализации
- ⏯️ Пауза/продолжить/остановить

### Демонстрация в консоли
```bash
python main.py demo --games 10 --verbose
```

## 🧠 Архитектура нейросети

### Креативные особенности:

1. **Dueling DQN Architecture**
   - Разделение на Value Stream (оценка состояния) и Advantage Stream (преимущество действий)
   - Более эффективное обучение на состояниях, где выбор действия не так важен

2. **Spatial Attention Mechanism**
   - Нейросеть учится фокусироваться на важных областях поля
   - Выделяет паттерны слияния и угловые позиции

3. **2D Positional Encoding**
   - Синусоидальное кодирование позиций по аналогии с Transformer
   - Помогает сети понимать пространственные отношения

4. **Residual Connections**
   - Глубокая сеть с 3 convolutional блоками
   - Skip connections для стабильного градиентного потока

5. **Priority Experience Replay**
   - Приоритизация важного опыта на основе TD-ошибки
   - Importance sampling для коррекции bias

6. **Креативная система наград:**
   - Награда за слияние (логарифмическая шкала)
   - Бонус за пустые клетки
   - Бонус за максимальный тайл в углу
   - Оценка монотонности поля
   - Оценка гладкости (минимизация резких переходов)

### Структура модели:
```
Input: 4x4 board + 9 features
    ↓
Tile Embedding (Conv2d 1→64)
    ↓
Positional Encoding 2D
    ↓
ConvBlock (64→128) + Attention
    ↓
ConvBlock (128→256) + Attention
    ↓
ConvBlock (256→256) + Attention
    ↓
Global Average Pooling
    ↓
Concatenate with Feature Encoder
    ↓
┌─────────────────┬─────────────────┐
│  Value Stream   │ Advantage Stream│
│   (→ 1 value)   │   (→ 4 actions) │
└────────┬────────┴────────┬────────┘
         │                 │
         └────── Q = V + A - mean(A) ──→ Output
```

**Всего параметров:** ~2.6M

## 📊 Дополнительные признаки (Features)

Помимо игрового поля, сеть получает 9 дополнительных признаков:
1. Доля пустых клеток
2. Максимальный тайл (нормализованный)
3. Монотонность поля
4. Гладкость (smoothness)
5. Максимальный тайл в углу (0/1)
6-9. Маска допустимых ходов (UP, DOWN, LEFT, RIGHT)

## 📁 Структура проекта

```
2048-ai/
├── main.py              # Главный файл запуска
├── game_2048.py         # Логика игры 2048
├── neural_network.py    # Нейросеть (Dueling DQN + Attention)
├── trainer.py           # Система обучения
├── gui.py               # Графический интерфейс (Pygame)
├── requirements.txt     # Зависимости
├── README.md           
├── models/              # Сохранённые модели
│   ├── model_best.pt
│   └── model_final.pt
└── logs/                # Логи обучения
    └── training_log.json
```

## 🍎 Оптимизация для Apple Silicon

Проект автоматически определяет и использует:
- **MPS (Metal Performance Shaders)** на Apple Silicon (M1/M2/M3)
- **CUDA** на NVIDIA GPU
- **CPU** если ускорители недоступны

Ожидаемая производительность на M1/M2:
- ~300-500 эпизодов/минуту при обучении
- ~60+ FPS в режиме игры

## 💡 Советы по обучению

1. **Начните с быстрого обучения:**
   ```bash
   python main.py train --quick
   ```
   Это даст базовое понимание процесса за ~5 минут.

2. **Полное обучение:**
   ```bash
   python main.py train --episodes 10000
   ```
   Занимает ~30-60 минут на Apple Silicon.

3. **Для достижения 2048:**
   Обычно требуется 20,000-50,000 эпизодов обучения.

4. **Мониторинг прогресса:**
   - Следите за `Avg Score` и `Avg Max Tile`
   - Хорошие признаки: растущий средний счёт, появление 512/1024 тайлов

## 📈 Ожидаемые результаты

| Эпизоды | Средний счёт | Макс. тайл (типично) |
|---------|-------------|---------------------|
| 500     | ~500        | 128-256             |
| 2,000   | ~2,000      | 256-512             |
| 10,000  | ~5,000      | 512-1024            |
| 50,000  | ~10,000+    | 1024-2048           |

## 🛠 Отладка

### Проверка устройства:
```python
python -c "from neural_network import device; print(f'Using: {device}')"
```

### Тест игры:
```bash
python main.py quick
```

### Тест нейросети:
```bash
python neural_network.py
```

## 📝 Лицензия

MIT License - свободное использование.

---

**Создано с ❤️ и нейросетями**
