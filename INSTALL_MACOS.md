# 🍎 Установка на macOS

## Проблема: Python от Homebrew без Tkinter

Если вы видите ошибку `ModuleNotFoundError: No module named '_tkinter'`, это значит что Python от Homebrew собран без поддержки GUI.

## ✅ Решения (выберите одно)

### Вариант 1: Терминальный интерфейс (работает сейчас!)

Игра автоматически переключится на красивый терминальный интерфейс:

```bash
python main.py play
```

**Управление в терминале:**
- `↑↓←→` — стрелки для движения
- `r` — рестарт
- `a` — включить AI
- `q` — выход

---

### Вариант 2: Установить Python с Tkinter

#### 2.1. Через официальный установщик (рекомендуется)

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. Установите (включает Tkinter автоматически)
3. Создайте venv:
   ```bash
   /usr/local/bin/python3.12 -m venv venv
   source venv/bin/activate
   pip install torch numpy
   python main.py play
   ```

#### 2.2. Через pyenv (для продвинутых)

```bash
# Установить зависимости для Tkinter
brew install tcl-tk

# Установить pyenv
brew install pyenv

# Установить Python с Tkinter
env \
  PATH="$(brew --prefix tcl-tk)/bin:$PATH" \
  LDFLAGS="-L$(brew --prefix tcl-tk)/lib" \
  CPPFLAGS="-I$(brew --prefix tcl-tk)/include" \
  PKG_CONFIG_PATH="$(brew --prefix tcl-tk)/lib/pkgconfig" \
  CFLAGS="-I$(brew --prefix tcl-tk)/include" \
  PYTHON_CONFIGURE_OPTS="--with-tcltk-includes='-I$(brew --prefix tcl-tk)/include' --with-tcltk-libs='-L$(brew --prefix tcl-tk)/lib -ltcl8.6 -ltk8.6'" \
  pyenv install 3.12.0

# Использовать эту версию
pyenv local 3.12.0
python -m venv venv
source venv/bin/activate
pip install torch numpy
```

#### 2.3. Пересобрать Homebrew Python с Tkinter

```bash
# Установить tcl-tk
brew install tcl-tk

# Пересобрать python с флагами
brew reinstall python@3.12 --with-tcl-tk
```

---

### Вариант 3: Использовать системный Python (macOS встроенный)

macOS обычно идёт с Python 3 + Tkinter:

```bash
# Проверить
/usr/bin/python3 -c "import tkinter; print('OK')"

# Если работает:
/usr/bin/python3 -m venv venv
source venv/bin/activate
pip install torch numpy
python main.py play
```

---

## 🧪 Проверка Tkinter

```bash
python -c "import tkinter; tkinter.Tk().mainloop()"
```

Должно открыться пустое окно. Если открылось — Tkinter работает!

---

## 🎮 Режимы игры

### GUI (если Tkinter работает)
```bash
python main.py play          # Ручная игра
python main.py play --ai     # AI играет
```

### Терминал (работает всегда)
```bash
python gui_terminal.py       # Ручная игра в терминале
python gui_terminal.py --ai  # AI играет в терминале
```

### Консоль (без интерфейса)
```bash
python main.py demo --games 10  # Просмотр статистики AI
```

---

## 🚀 Обучение нейросети

Работает независимо от GUI:

```bash
# Быстрое обучение (500 эпизодов, ~2 мин)
python main.py train --quick

# Полное обучение (5000 эпизодов, ~15 мин на Apple Silicon)
python main.py train

# Кастомное обучение
python main.py train --episodes 10000 --lr 0.0001
```

---

## 💡 Рекомендация

**Для macOS Apple Silicon:**
1. Используйте **терминальный интерфейс** (работает из коробки)
2. Или установите официальный Python с python.org (проще всего)

**Терминальный интерфейс** поддерживает:
- ✅ Цветные плитки
- ✅ Интерактивное управление стрелками
- ✅ AI режим
- ✅ Красивое отображение счёта

GUI даёт только визуальные преимущества, функциональность идентична.
